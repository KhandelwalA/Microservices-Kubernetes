spring:
  application:
    name: investment
  cloud: 
    kubernetes: 
      config: 
        namespace: default         # K8s namespace where to look for config map 
        name: investment-config-map   # Name of the config map where to look for the properties