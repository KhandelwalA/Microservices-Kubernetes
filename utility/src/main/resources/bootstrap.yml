spring:
  application:
    name: utility
  cloud: 
    kubernetes: 
      config: 
        namespace: default         # K8s namespace where to look for config map 
        name: utility-config-map   # Name of the config map where to look for the properties